# Archived Flask tracker

This prototype tracked `setback` and `recovery` event counts and experimental practice-distribution metrics. Catalyst Grit v1.0.1 replaced it with the canonical recovery-record engine.

The code is retained only to preserve repository history. It is unsupported, not packaged, not tested as a production surface, and must not be reintroduced without a documented architecture and ethics review.
