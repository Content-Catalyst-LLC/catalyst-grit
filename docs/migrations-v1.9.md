# Migration 008 — Publication, API, and Institutional Governance

Migration 008 adds institutional policies, append-only access reviews, scoped API clients and rate windows, append-only API request audit events, publication artifacts and event history, methodology registration, and schema-deprecation declarations.

The migration does not alter or recompute existing recovery records, revisions, monitoring snapshots, evidence, assumptions, handoffs, team contributions, or learning history. Rollback removes only the v1.9 institutional tables. Existing v1.8 workspace data remains intact.
