# Catalyst Grit WordPress v1.4.0

Shortcodes:

- `[catalyst_grit_demo]` — non-persistent browser condition-mapping demo.
- `[catalyst_grit_workspace]` — authenticated, per-user private workspace bundle storage.

The demo exposes keyboard-accessible controls and output panels for pressure, constraints, supports, missing context, contradictions, components, flags, actions, and canonical JSON.
