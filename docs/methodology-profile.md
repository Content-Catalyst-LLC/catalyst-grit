# Methodology Profile v1.3.0

The default profile is stored at `methodology/recovery-profile-v1.3.0.json`. The weighted component calculation remains stable for longitudinal compatibility. v1.3.0 adds condition maps, source-linked review flags, completeness, contradiction, and score-display policy outputs around that calculation.
